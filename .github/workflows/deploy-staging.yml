name: Deploy Staging

on:
  push:
    branches: [develop]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create .env.staging from repo secrets
        run: |
          cat > .env.staging << 'EOF'
          VITE_API_BASE=${{ secrets.STAGING_VITE_API_BASE }}
          VITE_REQUISITIONS_SCRIPT=${{ secrets.STAGING_VITE_REQUISITIONS_SCRIPT }}
          VITE_INVENTORY_SCRIPT=${{ secrets.STAGING_VITE_INVENTORY_SCRIPT }}
          VITE_GOOGLE_SCRIPT=${{ secrets.STAGING_VITE_GOOGLE_SCRIPT }}
          VITE_CRM_SCRIPT=${{ secrets.STAGING_VITE_CRM_SCRIPT }}
          EOF

      - name: Build for staging
        run: npm run build:staging

      - name: Prepare .htaccess for SPA routing
        run: cp .htaccess dist/.htaccess

      - name: Deploy to Hostinger staging
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_STAGING_SERVER }}
          username: ${{ secrets.FTP_STAGING_USERNAME }}
          password: ${{ secrets.FTP_STAGING_PASSWORD }}
          local-dir: dist/
          server-dir: /
          dangerous-clean-slate: true
